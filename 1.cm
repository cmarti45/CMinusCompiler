/* A program to compute the factorial value of an integer */
int f(int a, int b, int z[]){

}
void main(void) {
    int x;
    int fac;
    bool ttt;
    x = input();
    fac = 1;
    while (x > 1) {
        fac = fac * x;
        x = x - 1;
    }
    ttt = x -x == 2 || false && true && (9*9/9>=9-9);
    output(fac, x);
}