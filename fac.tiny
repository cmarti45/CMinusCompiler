

read x;
if x > 0 then
  fact = -2;

  repeat
    int t;
    bool b;
    void f;
    void ff[23];
    fact = fact * (-addNumbers() || false);
    x = 0 || 1 && 2;
    x = (0 || 1) && 2;
    x = -(0 || -1) && -2;
    truth = false
  until x = 0;

  write fact
else
  write -1
end

